import React from "react";
//Custom components are always starting with upper case and html components are always lower case
function Header(){
    return <header>
        <h1>Keeper </h1>
    </header>
}

export default Header;